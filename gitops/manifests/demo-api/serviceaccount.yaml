apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo-api
  namespace: demo-api
  annotations:
    # Reference value only. A real apply would set this to the
    # demo_api_irsa_role_arn output from infrastructure/iam.tf.
    eks.amazonaws.com/role-arn: "arn:aws:iam::123456789012:role/agentic-devsecops-demo-demo-api-irsa"
